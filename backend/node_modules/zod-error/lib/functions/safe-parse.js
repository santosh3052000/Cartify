"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.safeParse = safeParse;
const generate_error_message_1 = require("./generate-error-message");
/**
 * Safe parses a Zod schema.
 * @export
 * @template T
 * @param {T} schema
 * @param {unknown} data
 * @param {ErrorMessageOptions} [options]
 * @return {*}  {SafeParseReturnType<T['_output']>}
 */
function safeParse(schema, data, options) {
    const result = schema.safeParse(data);
    if (!result.success) {
        const message = (0, generate_error_message_1.generateErrorMessage)(result.error.issues, options);
        return { success: false, error: { message } };
    }
    return {
        success: true,
        data: result.data,
    };
}
