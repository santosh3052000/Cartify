import { Breadcrumbs } from '../types';
import { z } from 'zod';
/**
 * Adds breadcrumbs to a path.
 * Delimiter defaults to >.
 * @export
 * @param {z.core.$ZodIssue['path']} path
 * @param {Breadcrumbs} options
 * @return {*}  {string}
 */
export declare function getBreadcrumbs(path: z.core.$ZodIssue['path'], options: Breadcrumbs): string;
